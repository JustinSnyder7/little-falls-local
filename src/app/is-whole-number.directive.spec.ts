import { IsWholeNumberDirective } from './is-whole-number.directive';

describe('IsWholeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new IsWholeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
