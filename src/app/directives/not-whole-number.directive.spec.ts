import { NotWholeNumberDirective } from './not-whole-number.directive';

describe('NotWholeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NotWholeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
